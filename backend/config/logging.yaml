# 日志配置：级别、格式、输出目标、轮转策略
# 所有模块通过 logging.getLogger(__name__) 获取 logger，自动继承 root logger 配置

level: INFO
format: "%(asctime)s | %(levelname)-8s | %(name)s | %(message)s"
datefmt: "%Y-%m-%d %H:%M:%S"
file_path: logs/app.log
file_max_bytes: 10485760  # 10MB
file_backup_count: 5
console_enabled: true
